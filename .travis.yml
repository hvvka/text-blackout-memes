language: python

install:
  - pip install -r requirements.txt

script:
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
  - docker build -t  $DOCKER_USERNAME/cross-out-memes:latest .
  - docker push $DOCKER_USERNAME/cross-out-memes
